[defaults]
inventory = inventory.ini
roles_path = roles
# The playbook runs with connection: local, so no SSH host keys are involved.
host_key_checking = False
# Still auto-discovers the Python interpreter, without the discovery warning.
interpreter_python = auto_silent
# The default output callback, with task results formatted as YAML instead of JSON.
stdout_callback = default
callback_result_format = yaml
