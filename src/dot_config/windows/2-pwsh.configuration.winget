## WARNING: This file is chezmoi-managed and should not be manually edited!

# yaml-language-server: $schema=https://aka.ms/configuration-dsc-schema/0.3
$schema: https://raw.githubusercontent.com/PowerShell/DSC/main/schemas/2023/08/config/document.json
metadata:
  winget:
    processor:
      identifier: dscv3
resources:
  - type: Microsoft.DSC.Transitional/RunCommandOnSet
    name: ExecutionPolicyWindowsPowerShell
    properties:
      executable: powershell
      arguments:
        - -NoProfile
        - -NoLogo
        - -Command
        - "Set-ExecutionPolicy -ExecutionPolicy Unrestricted -Scope CurrentUser -Force"
  - type: Microsoft.DSC.Transitional/RunCommandOnSet
    name: ExecutionPolicyPwsh
    properties:
      executable: pwsh
      arguments:
        - -NoProfile
        - -NoLogo
        - -Command
        - "Set-ExecutionPolicy -ExecutionPolicy Unrestricted -Scope CurrentUser -Force"
  - type: Microsoft.PowerShell.PSResourceGet/PSResourceList
    name: WindowsDscModules
    metadata:
      winget:
        description: >-
          Requires PSResourceGet 1.3.0-preview1+ to already be installed before winget configure
          applies this file -- see 1-init.configuration.winget, which installs it as its own
          separate 'winget configure' invocation beforehand. This can't be bootstrapped by an
          earlier resource in this same file: winget's dscv3 processor resolves every declared
          resource type up front, before executing any resource's Set, so an in-file bootstrap
          resource would already be too late for this resource's own resolution to succeed
          (confirmed via a real failure on a fresh machine).
    properties:
      repositoryName: PSGallery
      resources:
        - name: Microsoft.Windows.Settings
          preRelease: true
        - name: Microsoft.Windows.Developer
          preRelease: true
