---
# Homebrew taps
homebrew_taps:
  - domt4/autoupdate

# homebrew-autoupdate schedule/flags — see https://github.com/DomT4/homebrew-autoupdate
# Runs every 43200 seconds (12h), upgrading + cleaning up automatically.
homebrew_autoupdate_args: "43200 --upgrade --cleanup --immediate --sudo"
{{/* This value is Jinja2, evaluated by Ansible at playbook-run time -- the {{ "{{" }} / {{ "}}" }}
     escapes stop chezmoi's Go templates (same {{ }} delimiters) from trying to parse it too. */}}
homebrew_autoupdate_plist: "{{ "{{" }} ansible_facts.env.HOME {{ "}}" }}/Library/LaunchAgents/com.github.domt4.homebrew-autoupdate.plist"

# Apps pinned to the Dock, left to right, as full paths to their .app
# bundle. This replaces the Dock's whole pinned-app list (including
# whatever macOS ships by default), so leave nothing off the list you want
# to keep. Finder isn't included — it's a fixed icon macOS always shows
# first, not something in this list.
dock_pinned_apps:
  - /Applications/System Settings.app
  - /Applications/Utilities/Activity Monitor.app
  - /Applications/Ghostty.app
  - /Applications/Microsoft Outlook.app
  - /Applications/Messages.app
  - /Applications/Discord.app
  - /Applications/Firefox.app
  - /Applications/Brave Browser.app
  - /Applications/TickTick.app
  - /Applications/AnyList.app
  - /Applications/Obsidian.app
  - /Applications/VSCodium.app
  - /Applications/Visual Studio Code.app
  - /Applications/Music.app
  - ~/Applications/Chromium Apps.localized/YouTube.app

# Formulas (CLI tools / non-GUI packages)
homebrew_formulas:
  - chezmoi
  - coreutils
  - curl
  - dockutil
  - fzf
  - git
  - jq
  - mas
  - micro
  - mise
  - pinentry-mac
  - sheldon
  - starship
  - utiluti
  - yq

# Casks (GUI apps / fonts)
homebrew_casks:
  - 1password-cli
  - alt-tab
  - anylist
  - brave-browser
  - discord
  - docker-desktop
  - firefox
  - font-cascadia-mono-nf
  - ghostty
  - git-credential-manager
  - keka
  - localsend
  - macshot
  - megasync
  - obsidian
  - stats
  - thaw
  - ungoogled-chromium
  - visual-studio-code
  - vlc
  - vscodium

# Mac App Store apps: name -> App Store numeric id
mac_app_store_apps:
  1Password: 443987910
  Amphetamine: 937984704
  Clocker: 1056643111
  Kindle: 302584613
  "Microsoft 365": 1450038993
  Paprika: 1303222628
  "Poolsuite FM": 1514817810
  Speedtest: 1153157709
  TestFlight: 899247664
  TickTick: 966085870
  "UTM Virtual Machines": 1538878817
  WireGuard: 1451685025
  "Windows App": 1295203466
  Xcode: 497799835
  "Yubico Authenticator": 1497506650

# PWAs to install in ungoogled-chromium via WebAppInstallForceList policy. Sourced from
# .chezmoidata/pwas.toml, shared with the Windows winget PWA provisioning (see
# 8.pwas.configuration.winget.tmpl) so both platforms force-install the same list.
#
# Name and icon come from the site's web app manifest — don't set
# custom_name/custom_icon here. If Chromium's hidden background install
# can't load the page, it installs a URL-named placeholder app (that's
# what once produced `https/www.youtube.com.app` with no icon); the
# chromium_pwas role detects placeholders and navigates a real tab to the
# install URL, which makes Chromium swap in the real manifest-derived app.
# {{ $pwas := list }}{{ range .pwas.entries }}{{ if not (has "darwin" (index . "ignore_on" | default list)) }}{{ $pwas = append $pwas (omit . "ignore_on") }}{{ end }}{{ end }}
chromium_web_apps:
{{ $pwas | toYaml | trimSuffix "\n" | indent 2 }}
